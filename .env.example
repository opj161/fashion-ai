# Add at least one Gemini API key 
# Multiple keys can be added for redundancy and higher quota
GEMINI_API_KEY_01=your_primary_gemini_api_key_here
GEMINI_API_KEY_02=your_fallback_gemini_api_key_here
# Add more keys as needed with GEMINI_API_KEY_03, etc.

# Server configuration
HOST_IP=localhost        # Change to your server's IP address if accessing from other devices
BACKEND_PORT=5002        # Backend API port
FRONTEND_PORT=8080       # Frontend web port
DEBUG=false              # Set to true for debug mode

# Optional: Override API URL (usually not needed with Docker networking)
# API_URL=http://backend:5002

# Optional: CORS allowed origins (comma-separated, usually not needed)
# ALLOWED_ORIGINS=http://yourwebsite.com,http://localhost:8080